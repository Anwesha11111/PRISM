version: '3.8'

services:
  openclaw:
    build: .
    ports:
      - "8080:8080"
    environment:
      - OPENCLAW_AUTH_TOKEN=${OPENCLAW_AUTH_TOKEN:-prism-secret}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./agents:/app/agents
      - ./policies.yaml:/app/policies.yaml
